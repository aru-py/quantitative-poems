% Creates book for publishing (with gutters)

\usepackage{geometry}
\geometry{
    paperwidth=6in,
    paperheight=9in,
    left=0.75in, % Gutter or inner margin
    right=0.5in, % Outer margin
    top=0.75in,
    bottom=0.75in,
    bindingoffset=0.25in % Additional space for the binding; adjust as needed
}